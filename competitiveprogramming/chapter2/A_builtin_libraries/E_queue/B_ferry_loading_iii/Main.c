#include <stdio.h>
#include <string.h>

int main() {
	int T, i;
	scanf("%d", &T);
	for (i = 0; i < T; i++) {
		int n, t, m, j;
		scanf("%d %d %d", &n, &t, &m);
		for (j = 0; j < m; j++) {
			int aTime;
			char pos[10];
			scanf("%d %s", aTime, pos);
			if 
		}

	}
	return 0;
}